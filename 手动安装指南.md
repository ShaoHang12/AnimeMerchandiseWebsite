# 手动安装指南

## 问题诊断

如果遇到"拒绝访问"或下载失败，请按以下步骤操作：

## 方案一：手动下载 Maven Wrapper（推荐）

### 步骤1：下载 maven-wrapper.jar
1. 用浏览器打开：https://repo.maven.apache.org/maven2/org/apache/maven/wrapper/maven-wrapper/3.1.0/maven-wrapper-3.1.0.jar
2. 保存文件到：anime_mall\.mvn\wrapper\maven-wrapper.jar

### 步骤2：运行启动脚本
双击运行 "启动后端.bat"

## 方案二：手动安装 Maven

### 步骤1：下载 Maven
1. 访问：https://maven.apache.org/download.cgi
2. 下载 Binary zip archive（apache-maven-3.9.x-bin.zip）

### 步骤2：解压 Maven
1. 解压到目录，例如：D:\apache-maven-3.9.6
2. 确保目录结构如下：
   `
   D:\apache-maven-3.9.6\
   ├── bin\
   ├── boot\
   ├── conf\
   └── lib\
   `

### 步骤3：配置环境变量（可选）
1. 添加系统变量：MAVEN_HOME = D:\apache-maven-3.9.6
2. 添加到PATH：%MAVEN_HOME%\bin

### 步骤4：运行构建
`ash
cd anime_mall
D:\apache-maven-3.9.6\bin\mvn clean package -DskipTests
java -jar target\anime_mall-0.0.1-SNAPSHOT.jar
`

## 方案三：使用 IntelliJ IDEA 运行

### 步骤1：打开项目
1. 用 IntelliJ IDEA 打开 anime_mall 目录
2. IDEA 会自动识别为 Maven 项目

### 步骤2：配置运行
1. 找到 SpringbootSchemaApplication.java
2. 右键选择 "Run"

### 步骤3：配置数据库
确保 application-dev.yml 中的数据库配置正确。

## 常见错误

### 错误1：无法连接到远程服务器
原因：网络问题，无法下载依赖
解决：
- 使用VPN或代理
- 手动下载依赖（见方案一）
- 使用IntelliJ IDEA（见方案三）

### 错误2：拒绝访问
原因：权限不足
解决：
- 以管理员身份运行脚本
- 检查文件是否被占用
- 检查杀毒软件是否阻止

### 错误3：找不到Java
原因：JAVA_HOME未设置
解决：
- 确保已安装JDK 21+
- 设置JAVA_HOME环境变量
- 或使用启动脚本（已自动配置）
