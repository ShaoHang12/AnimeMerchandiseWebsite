# 动漫产品商城 - 快速启动指南

## 前置条件检查

### 1. 检查Java环境
项目使用 JDK 21+，已配置使用 IntelliJ IDEA 的 JBR (JDK 25)。
如果未安装 IntelliJ IDEA，请下载 JDK 21+: https://adoptium.net/

### 2. 检查Node.js环境
打开命令行，运行: node -v
如果没有安装Node.js，请下载: https://nodejs.org/

### 3. 检查MySQL环境
确保MySQL服务已启动，并知道root密码。

## 快速启动步骤

### 步骤1: 配置数据库

1. 打开文件: anime_mall/src/main/resources/application-dev.yml
2. 修改第18行的密码:
   `yaml
   password: 你的MySQL密码
   `

3. 初始化数据库:
   - 打开命令行，进入项目目录
   - 运行: mysql -u root -p < anime_mall/anime_mall.sql
   - 输入MySQL密码

### 步骤2: 启动后端服务

**方式A: 使用一键启动脚本（推荐）**
- 双击运行 "一键启动.bat"
- 选择选项 [2] 仅启动后端服务

**方式B: 手动启动**
`ash
cd anime_mall
set "JAVA_HOME=D:\Download\Work\IntelliJ IDEA 2026.1.2\jbr"
mvnw.cmd clean package -DskipTests
java -jar target\anime_mall-0.0.1-SNAPSHOT.jar
`

等待看到 "Started SpringbootSchemaApplication" 字样表示启动成功。

### 步骤3: 启动前端服务

**方式A: 使用一键启动脚本（推荐）**
- 双击运行 "一键启动.bat"
- 选择选项 [3] 启动管理后台 或 [4] 启动前台商城

**方式B: 手动启动**

启动管理后台:
`ash
cd vue\admin\admin
npm install
npm run serve
`

启动前台商城:
`ash
cd vue\front\front
npm install
npm run serve -- --port 8082
`

### 步骤4: 访问系统

- 后端API: http://localhost:8080/anime_mall/
- 管理后台: http://localhost:8081
- 前台商城: http://localhost:8082

## 默认账号

### 管理员
- 用户名: admin
- 密码: admin123

### 测试用户
- 账号: user1
- 密码: 123456


## 测试构建

如果想先测试项目是否能正常构建，可以：
1. 双击运行 "测试构建.bat"
2. 等待构建完成
3. 如果看到 "构建成功！" 字样，说明项目配置正确

## 常见问题

### Q: 启动后端报错 "数据库连接失败"
A: 检查 application-dev.yml 中的数据库配置是否正确。

### Q: npm install 失败
A: 使用淘宝镜像: npm install --registry=https://registry.npmmirror.com

### Q: 端口被占用
A: 修改配置文件中的端口号，或关闭占用端口的程序。

### Q: 前端页面空白
A: 确保后端服务已启动，并且端口配置正确。

### Q: Java版本不兼容
A: 项目需要 JDK 21+，已配置使用 IntelliJ IDEA 的 JBR。

